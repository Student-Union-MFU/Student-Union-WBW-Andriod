# Committed fallback for the secrets-gradle-plugin.
# The REAL key lives in local.properties as MAPS_API_KEY (gitignored) and overrides this.
# This non-empty placeholder only exists so a checkout without a key still compiles; with
# it the map loads no tiles and the log warns, instead of failing the build.
MAPS_API_KEY=MISSING_MAPS_API_KEY
